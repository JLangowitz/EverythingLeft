//- Profile Page

extends layout

block scripts
	script(src='javascripts/username.js')
	script(src='javascripts/profile.js')

block content
	h1(style='padding-left: 100px;') #{name}
	div(class='text-center hero-unit float-two')
		div(class='text-left')
			h3 Dietary Restrictions
			if (dietary.length > 0)
				each diet in dietary
					| #{diet.name}
					br
			else
				| [none]
			h3 Cuisines
			if (cuisines.length > 0)
				each cuis in cuisines
					| #{cuis.name}
					br
			else
				| [none]
			h3 Flavors
			if (flavors.length > 0)
				each flav in flavors
					| #{flav.name}
					br
			else
				| [none]
		br
		br
		form(id='settings', class='form-inline')
			include _multiselect
			button(class='btn btn-inverse btn-chosen', type='submit') Save Preferences
			span(class='help-block') Selected tags will become your default search parameters. You can return here to change them at any time or customize individual searches on our search page.
		form(class='form-inline', id='setuser')
			div(class="input-append", id='error-append')
				input(type="text", id="username", placeholder="username", name='username')
				button(type="submit", class='btn btn-inverse') Change Username
				//- div(class="form-actions")
	div(class="favs float-one")
		h2 Favorites
		if (favorites.length > 0)
				each recipe in favorites
					include _parrecipe
			else
				p You have no favorites yet!

	//- div(id="my_ops")
	//- 	div(value=preferences, id="preferences")
	//- 		each pref, i in preferences
	//- 			| #{pref}
	//- 	input(type="button", value="Change Settings")

	//- div(id="liked")
	//- 	each fav, i in favorites
	//- 		| #{fav}